<section class="wrapper">
  <h1>Choose Your World</h1>
<%= form_for(Room.with_generated_id) do |f| %>
  <%= f.collection_select(:map_id, Map.all, :id, :name) %>
  <%= f.hidden_field :id %>
  <%= f.submit%>
<% end %>
</section>
